# Ansible variables coming from Terraform
#   This is an auto-generated file.

ansible_python_interpreter: ${ansible_python_interpreter}
banner_text_file: ${banner_text_file}
centos_user_password: ${centos_user_password}
password_max_days: ${password_max_days}
password_min_days: ${password_min_days}
timestamp: ${timestamp()}
sha_crypt_max_rounds: ${sha_crypt_max_rounds}
sha_crypt_min_rounds: ${sha_crypt_min_rounds}
ssh_user: "${ssh_user}"
